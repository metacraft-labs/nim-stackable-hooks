nimcache/
build/
*.exe
*.dll
*.so
*.dylib
*.pdb
*.obj
*.o
*.a
*.lib
*.exp
test-logs/

# Compiled Nim test executables (the `.nim` sources are tracked; the binaries
# `nim c -r` drops next to them are build artifacts).
tests/test_smoke
tests/test_hook_registry_priority_order
tests/test_reentrancy_guard_prevents_recursion
tests/test_linux_raw_syscalls

# Direnv environment cache
.direnv/

# Reprobuild environment cache
.repro/
